import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    marginTop: 50,
  },
  input: {
    backgroundColor: "#bbb",
  },
  listItem: {
    backgroundColor: "#ccc",
  },
});

export default styles;
